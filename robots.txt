# RUŌOD — https://ruood.com/
#
# This file is served at the root of the domain, which is the only place a
# crawler reads one. It therefore speaks for the whole of ruood.com, including
# paths served by other origins through a proxy.

User-agent: *
Allow: /

Sitemap: https://ruood.com/sitemap.xml

# ── RUŌOD Lab ─────────────────────────────────────────────────────────────
#
# The Lab is a separate project on a separate Netlify site, reached at
# https://ruood.com/lab because this site proxies /lab through to it (see
# netlify.toml). Because robots.txt is only honoured at the root of a domain,
# the Lab cannot publish its own — its rules belong here. Its project keeps a
# robots.txt of its own purely as the source to merge in; the Allow above
# already covers /lab, so the Sitemap line is all that needs carrying over.

Sitemap: https://ruood.com/lab/sitemap.xml
